/**
 * Publishes a body of something for reading: markdown, a diff, a document, a record about a thing.
 * Each component binds a recipe a theme can extend and draws nothing of its own. The recipes reach
 * an application's compiler through the preset under `./theme`, and the components reach its
 * bundle through here.
 *
 * @packageDocumentation
 */
