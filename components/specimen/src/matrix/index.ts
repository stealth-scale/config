/**
 * Publishes the matrix: one captioned cell per value of an axis.
 */

export { type Axis, captionOf, nameOf } from "#matrix/axis.ts";
export { Matrix, type MatrixProps } from "#matrix/matrix.tsx";
