/**
 * Publishes how a row of actions folds: the rules it folds by, and the priority an action states.
 */

export { FOLDED, FOLDING, PRIORITY } from "#folding/folding.ts";
export { PRIORITIES, type Priority } from "#folding/priority.ts";
