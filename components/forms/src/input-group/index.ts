/**
 * Publishes the input group's parts, which a caller composes as `InputGroup.Root` holding a field
 * and a mark at one end or both.
 */

export { End, type EndProps } from "#input-group/end.ts";
export { Field, type FieldProps } from "#input-group/field.ts";
export { Root, type RootProps } from "#input-group/root.ts";
export { Start, type StartProps } from "#input-group/start.ts";
