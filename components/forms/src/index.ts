/**
 * Publishes what composes controls into something a person submits, and what reports what is
 * wrong with it. Each component binds a recipe a theme can extend and draws nothing of its own. The
 * recipes reach an application's compiler through the preset under `./theme`, and the components
 * reach its bundle through here.
 *
 * @packageDocumentation
 */
