/**
 * Publishes what a person presses: a button, a toggle, and the triggers that carry an action.
 * Each component binds a recipe a theme can extend and draws nothing of its own. The recipes
 * reach an application's compiler through the preset under `./theme`, and the components reach
 * its bundle through here.
 *
 * @packageDocumentation
 */
