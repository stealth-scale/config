/**
 * Publishes what arranges what is already there: the boxes, the stacks, the rules between them and
 * the surfaces under them. Each component binds a recipe a theme can extend and draws nothing of
 * its own. The recipes reach an application's compiler through the preset under `./theme`, and the
 * components reach its bundle through here.
 *
 * @packageDocumentation
 */
