/**
 * Publishes the divider: one line between things, across the page or down a row.
 */

export { PropsProvider as DividerPropsProvider } from "#divider/context.ts";
export { Divider, type DividerProps } from "#divider/divider.ts";
