/**
 * Publishes the stack: children laid one after another along one direction, a gap apart.
 */

export { PropsProvider as StackPropsProvider } from "#stack/context.ts";
export { Stack, type StackProps } from "#stack/stack.ts";
