/**
 * Publishes the spacer: the room a stack has left over, taken by an element that draws nothing.
 */

export { PropsProvider as SpacerPropsProvider } from "#spacer/context.ts";
export { Spacer, type SpacerProps } from "#spacer/spacer.ts";
