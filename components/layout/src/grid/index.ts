/**
 * Publishes the grid: entries laid out in a count of equal columns or in as many columns of one
 * measure as there is room for, composed as `Grid.Root` holding `Grid.Item`.
 */

export { Item, type ItemProps } from "#grid/item.ts";
export { Root, type RootProps } from "#grid/root.ts";
