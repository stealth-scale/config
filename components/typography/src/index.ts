/**
 * Publishes the components that are text: a heading, a paragraph, a snippet of code, a key a
 * reader is asked to press. Each component binds a recipe a theme can extend and draws nothing of
 * its own. The recipes reach an application's compiler through the preset under `./theme`, and the
 * components reach its bundle through here.
 *
 * @packageDocumentation
 */
