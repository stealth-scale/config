/**
 * Publishes the mark: the run of words picked out of the text around it, and the provider that
 * sets the variants of every mark below it.
 */

export { PropsProvider as MarkPropsProvider } from "#mark/context.ts";
export { Mark, type MarkProps } from "#mark/mark.ts";
