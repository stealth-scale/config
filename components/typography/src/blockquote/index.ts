/**
 * Publishes the blockquote: a quotation with a mark beside it and a caption under it, composed as
 * `Blockquote.Root` holding `Blockquote.Icon`, `Blockquote.Content` and `Blockquote.Caption`.
 */

export { Caption, type CaptionProps } from "#blockquote/caption.ts";
export { Content, type ContentProps } from "#blockquote/content.ts";
export { Icon, type IconProps } from "#blockquote/icon.ts";
export { Root, type RootProps } from "#blockquote/root.ts";
