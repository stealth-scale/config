/**
 * Draws the shipping settings, which share a frame with the orders page.
 */

import { type ReactElement } from "react";

/**
 * Draws the page.
 *
 * @returns The settings.
 */
export function Shipping(): ReactElement {
  return <article>{"Shipping settings"}</article>;
}
