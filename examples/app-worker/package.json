{
  "name": "@stealthscale/example-app-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Totals amounts on a module worker whose file the dependency scan is pointed at explicitly.",
  "type": "module",
  "imports": {
    "#*": "./src/*"
  },
  "scripts": {
    "build": "vp build",
    "check": "vp check",
    "dev": "vp dev",
    "preview": "vp preview",
    "test": "vp test"
  },
  "dependencies": {
    "@stealthscale/example-lib-core": "workspace:^"
  },
  "devDependencies": {
    "@stealthscale/vite-config": "workspace:^",
    "@stealthscale/vite-config-typescript": "workspace:^",
    "@types/node": "catalog:",
    "happy-dom": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
