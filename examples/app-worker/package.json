{
  "name": "@stealthscale/example-app-worker",
  "version": "0.0.0",
  "private": true,
  "description": "An app that does its arithmetic off the main thread, in a worker bundled as a module.",
  "type": "module",
  "imports": {
    "#*": "./src/*"
  },
  "scripts": {
    "build": "vp build",
    "check": "vp check",
    "dev": "vp dev",
    "preview": "vp preview",
    "test": "vp test"
  },
  "dependencies": {
    "@stealthscale/example-lib-core": "workspace:^"
  },
  "devDependencies": {
    "@stealthscale/config-typescript": "workspace:^",
    "@stealthscale/config-vite": "workspace:^",
    "@types/node": "catalog:",
    "happy-dom": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
