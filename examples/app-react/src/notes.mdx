import { Badge } from "#badge.tsx";

# Release notes

This page is written in MDX. It imports a component from the same package and renders it inline.

- The badge below reads the manifest at build time: <Badge />
- Everything else on this page is markdown.
