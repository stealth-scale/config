/**
 * Draws the page somebody who has not signed in is sent to.
 */

import { type ReactElement } from "react";

/**
 * Asks a person to sign in.
 *
 * @returns The page.
 */
export function SignIn(): ReactElement {
  return <article>{"Sign in to read this."}</article>;
}
