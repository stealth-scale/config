/**
 * Publishes the keyboard shortcuts a page answers to. The provider is rendered innermost, because
 * a shortcut is answered by whatever is on screen, and a binding is written the way the platform
 * the person is on writes it.
 *
 * @packageDocumentation
 */

export * from "#tanstack.ts";
