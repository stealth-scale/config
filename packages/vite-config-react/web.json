{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "A fragment, not a tier: what React adds to the compiler options of the web tier. A consumer lists the tier first and this after it, in the array form of extends, so the fragment cannot be extended without the base by mistake.",
  "compilerOptions": {
    "jsx": "react-jsx"
  }
}
