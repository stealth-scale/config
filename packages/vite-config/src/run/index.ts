/**
 * Collects what a workspace root tells the task runner.
 */

export { cache } from "#run/cache.ts";
export { ci } from "#run/ci.ts";
export { type Doing, type Running } from "#run/settings.ts";
export { task } from "#run/task.ts";
