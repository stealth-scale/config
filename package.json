{
  "name": "@stealthscale/config",
  "version": "0.0.0",
  "private": true,
  "description": "Every configuration a stealth repository is built, checked, tested and released under.",
  "type": "module",
  "scripts": {
    "bootstrap": "cd packages/vite-plugin-base && vp pack && cd ../vite-plugin-sbom && vp pack && cd ../vite-config-core && vp pack && cd ../vite-config && vp pack && cd ../vite-config-react && vp pack && cd ../vite-config-css && vp pack",
    "ready": "pnpm run bootstrap && vp run ci"
  },
  "devDependencies": {
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@stealthscale/vite-config-react": "workspace:^",
    "@stealthscale/vite-config-typescript": "workspace:^",
    "@types/node": "catalog:",
    "eslint-plugin-jsdoc": "catalog:",
    "eslint-plugin-perfectionist": "catalog:",
    "eslint-plugin-tsdoc": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^12.4.1",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "^26",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@12.4.1"
}
